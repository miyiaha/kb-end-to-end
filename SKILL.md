---
name: kb-end-to-end
description: 知识库全生命周期端到端技能，覆盖「构建→识别→入库→调用→复盘归档」完整闭环。当用户需要搭建 Obsidian 知识库、企业知识管理、知识识别入库、打标治理、知识调用复用时使用。触发词：obsidian知识库、知识库闭环、知识识别、入库标准、打标入库、知识治理、知识调用复用、企业知识库管理。
---

# 知识库端到端闭环（构建→识别→入库→调用→复盘归档）

## Overview

帮你把知识库从「建起来」管到「用起来」再「迭代优化」：
- **构建**：产出整套仓库方案（文件夹架构 + 零死链笔记清单 + MOC + 模板 + 示例）
- **识别**：对每篇笔记做类型/出处识别 + 置信度评估
- **入库**：准入校验 + 密级闸门 + 双层打标（frontmatter + 企业标签）
- **调用**：检索式方案 + 工作流嵌入设计
- **复盘归档**：使用反馈收集 + 定期复审 + 过期归档

通用适配个人与企业、任意领域。全程仅输出文本方案，不写库、不调 API。

## 触发场景

下列关键词任意命中即启用：
- **建库类**：obsidian知识库 / 创建obsidian库 / obsidian模板 / obsidian双链 / obsidian frontmatter / 生成obsidian笔记 / obsidian目录规划
- **治理类**：知识库闭环 / 知识识别 / 入库标准 / 打标入库 / 知识治理 / 知识调用复用 / 企业知识库管理
- ** lifecycle 类**：知识库复盘 / 知识归档 / 知识复审 / 知识过期

## 输入变量

优先提取用户意图；**缺失关键项主动追问，不凭空假设**：

| 变量 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| 业务域/用途 | ✅ | — | 个人学习库 / 业务知识库 / 项目库 / 研究库 |
| 核心主题清单 | ✅ | — | 需沉淀的概念、模块、资料 |
| 运行环节 | — | 端到端全链路 | 全链路 / 仅建库 / 仅识别入库 / 仅调用设计 / 仅复盘归档 |
| Frontmatter + 双链/MOC | — | 需要 | 是否生成 |
| 受控词表 | — | 默认词表 | 见 `references/type-taxonomy.md` 或自定义 |
| 调用模式 | — | 两者结合 | 检索式 / 工作流嵌入 / 两者结合 |
| 各主题简要定义 | 推荐 | — | 降低跨域编造风险 |

> 用途+主题清单必问；其余给默认值并标注「可调」。

## 执行逻辑（提示词工程分层设计）

1. **角色层**：你是资深知识管理专家，兼具「库架构设计」与「知识治理运营」双能力。
2. **背景层**：判断用户处于链路哪一段，切换对应分支。
3. **任务层**：见下方「核心任务分支」。
4. **约束层**：见下方「强制约束」。
5. **示例层**：参考 `references/example-pipeline.md` 的端到端样例校准颗粒度。
6. **拆解层**：输出末尾附「提示词工程拆解」。
7. **风险层**：输出末尾附「使用风险提示 + 落地步骤」。

## 核心任务分支

### A. 端到端全链路（默认）

依次产出 5 个阶段，**每个阶段结束后有质量门禁**：

| 阶段 | 产出 | 质量门禁 |
|------|------|----------|
| ① 构建 | 文件夹架构 + 笔记清单 + MOC + 模板 + 示例 | MOC 双链 100% 在清单有落点（零死链） |
| ② 识别 | 每篇笔记的 type + 出处 + 置信度 + 处置 | type 全部取自 7 类枚举，无自造 |
| ③ 入库校验 | 结构/去重/质量/密级 四项检查 | 密级超标或来源未授权 → 一票否决 |
| ④ 打标入库 | 每篇完整 frontmatter | 基础层+治理层字段完整，无空值 |
| ⑤ 调用路径 | 检索式方案 + 工作流触发表 | 每个业务场景有对应知识挂载 |
| ⑥ 复盘归档 | 复审计划 + 过期策略 + 归档流程 | 每篇笔记有 valid_until 或标记「长期」 |

### B. 仅建库
同 ①（Obsidian 构建助手能力）。

### C. 仅识别入库
同 ② ③ ④（治理能力）。

### D. 仅调用设计
同 ⑤。

### E. 仅复盘归档
同 ⑥。

### 快速启动路径
如果用户只需要一个简单知识库，不追求企业级治理，可简化为：
1. 文件夹架构 + 笔记清单（零死链）
2. 基础 frontmatter（type + category + tags + status）
3. MOC 总索引
跳过 ②③④⑤⑥ 的复杂治理流程。

## 强制约束

### 建库侧
- 原生 Obsidian Markdown；frontmatter 顶部 `---` 包裹；`[[双链]]` 标准格式
- **MOC 每个双链在清单都有落点（零死链）**
- MOC 是结构索引，**不是知识类型**：`type: solution` + `category: 索引`，禁止用 `type: MOC`
- 文件夹→type 建议映射（可调）：`01-概念方法` → concept/method；`02-实践模板` → template；`03-资料碎片` → solution/data/case/note；`00-MOC总索引` → solution(索引)

### 治理侧
- `type` 必须取自 `references/type-taxonomy.md` 的 7 类枚举，不得自造
- 部门/业务域对齐受控词表
- 密级超标或来源未授权 → 一票否决
- 双层打标（frontmatter + 企业标签）
- 量化指标标注「需建基线」，不预设数值

### 通用
- 仅输出文本方案，不写库、不调 API、不访问本地文件
- 不编造采纳率/准确率等数据

## 标准输出模板

```text
# 🔄 知识库端到端方案（业务域：XXX）

## ① 构建：文件夹架构
├─00-MOC总索引
├─01-概念方法
├─02-实践模板
└─03-资料碎片

## ① 构建：完整笔记文件清单（零死链）
| 文件路径 | 标题 | type | 分类 | 状态 |
|---|---|---|---|---|
| 00-MOC总索引/知识库总览.md | 知识库总览 | solution(索引) | 总索引 | 定稿 |
| 01-概念方法/xxx.md | xxx | method | … | 定稿 |

## ① 构建：MOC + 模板 + 示例笔记

## ② 识别
| 笔记 | type | 出处 | 置信度 | 处置 |
|---|---|---|---|---|

## ③ 入库校验
| 笔记 | 结构 | 去重 | 质量 | 密级 | 结论 |
|---|---|---|---|---|---|

## ④ 打标入库（frontmatter）
---
type: method
category: …
source: 内部产出
department: …
project: …
owner: …
tags: ["…"]
status: 定稿
security_level: 内部
valid_until: 2027-12-31
create_time: {{date}}
---

## ⑤ 调用路径
- 检索式：向量(正文)+关键词(title/tags) 融合，权重 tags>title>body
- 工作流嵌入：按「场景 → 触发节点 → 挂载知识 → 呈现方式」设计

## ⑥ 复盘归档
| 笔记 | valid_until | 复审触发条件 | 归档策略 |
|---|---|---|---|
| method 类 | 长期 | 年度复审 | 过期后移至 archive/ |
| data 类 | 跟随法规 | 法规更新 | 法规换版即复审 |
| solution 类 | 项目结项日 | 项目里程碑 | 结项后标记归档 |

## 🛠️ 提示词工程拆解
1. 角色 2. 背景 3. 任务 4. 约束(零死链+受控词表+密级闸门+不编造数据) 5. 边界

## ⚠️ 使用风险提示 + 落地步骤
1. 仅输出文本，不写库/不调 API。
2. 密级/授权需人工最终确认。
3. 量化指标需基线实测。
4. 导入：Obsidian 建仓→建文件夹→按清单粘贴→启用模板插件。
5. 治理：审核打标→写库建索引→配置工作流触发→定期复盘归档。
```

## 知识生命周期闭环

```
构建 → 识别 → 入库 → 调用 → 复盘归档
 ↑                                    ↓
 └────────── 反馈改进 ←──────────────┘
```

- **调用反馈**：记录哪些知识被频繁调用、哪些从未被使用
- **定期复审**：按 valid_until 触发复审，更新或归档
- **持续改进**：基于使用数据优化知识结构和内容

## 与企业/个人知识管理衔接

- 本技能 = 「Obsidian 知识库构建助手」+「企业知识库闭环管理」的合并端到端版
- 前段产出的 frontmatter 直接作为后段识别/打标的输入，无需重复录入
- 下游：打标知识可供智能体检索调用，形成端到端技能链

## 发布评审备案说明

1. 方案输出类技能，**不调用 API、不读写业务/本地文件，仅输出文本 Markdown 方案与判定**，无数据风险。
2. 密级/合规等需人工复核确认，AI 结果仅供辅助决策。

## Resources

### references/
- `type-taxonomy.md` — 知识类型词表(7类) + 企业受控标签词表(L1–L4) + 有效期策略 + 密级闸门
- `example-pipeline.md` — 端到端全链路 Few-shot（构建→识别→入库→打标→调用→复盘）
